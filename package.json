{
  "name": "line-ferry",
  "displayName": "Line Ferry",
  "description": "Send the current line to the active terminal without executing (Ctrl+Shift+Enter)",
  "version": "0.0.1",
  "publisher": "tsukamoto",
  "repository": {
    "type": "git",
    "url": "https://github.com/tsukamoto/line-ferry"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sendLineToTerminal.send",
        "title": "Send Line to Terminal"
      }
    ],
    "keybindings": [
      {
        "key": "ctrl+shift+enter",
        "command": "sendLineToTerminal.send",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
